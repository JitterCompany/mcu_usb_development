enum GPIO_ID {
    GPIO_ID_LED_power,
    GPIO_ID_LED_running,
    GPIO_ID_LED_standby,
    GPIO_ID_LED_SDCard,

    GPIO_ID_SD_DETECT_INV,
    GPIO_ID_SDCARD_POWER_ENABLE,

    GPIO_ID_MAX // This should be last: it is used to count
};

